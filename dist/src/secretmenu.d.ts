import { CSSProperties, FC } from "react";
import { SecretMenuProps } from "./types";
export declare const formatFeatureName: (name: string) => string;
export declare const parseStyle: (styleString: string) => CSSProperties;
export declare const SecretMenu: FC<SecretMenuProps>;
